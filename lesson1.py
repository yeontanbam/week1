# student = {
#     'name': 'Nazgul',
#     'age': 18,
#     'year': None,
#     'subjects': {
#         'math': (23, 434, 324, 232),
#         'python': (3443, 42, 455),
#         'html': (23, 43, 54),
#         'css': (34, 345, 767, 78),
#     },
#     'total': None,
# }
# year = 2022 - student['age']
# math = sum(student['math'].value()) / len(student['math'].value())
# python = sum(student['python']) / len(student['python'])
# html = sum(student['html']) / len(student['html'])
# css = sum(student['css']) / len(student['css'])
# total = sum(student[math, python, html, css]) / len(student[math, python, html, css])
# print(year)

# list comprehentions
# генераторы списков
# num = [i for i in range(1, 1001)]
# print(num)
# num = [i for i in range(1, 1001) if i % 2 == 0]
# print(num)
# list = ['apple', 'cherry', 'orenge']
# fruit = [fruit if fruit != 'cherry' else 'melon' for fruit in list]
# print(fruit)

# from datetime


